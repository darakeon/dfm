@using DFM.Entities.Enums
@model AccountSign

@switch (Model)
{
	case AccountSign.Green:
		@("success")
		break;
	case AccountSign.Yellow:
		@("warning")
		break;
	case AccountSign.Red:
		@("danger")
		break;
	default:
		@("default")
		break;
}
