@model BaseMovesModel

@{
	var scheduleModel = Model as SchedulesCreateModel;
}

@if (scheduleModel != null)
{
	@await Html.PartialAsync("Modals/ScheduleRobotInterval")

	@await Html.PartialAsync("../Schedules/ExclusiveFields", scheduleModel)
	<hr/>
}
