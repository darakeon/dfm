@{
	var route = new RouteDefault();
}

<a href="@Url.RouteUrl(route.Name, new {action = "Config", controller = "Users", area = route.Area})">
	<span class="glyphicon glyphicon-cog" title="@Context.Translate("Settings")"></span>
</a>
