@model AccountSign
@switch (Model)
{
	case AccountSign.GreenLimit:
		@("success")
		break;
	case AccountSign.YellowLimit:
		@("warning")
		break;
	case AccountSign.RedLimit:
		@("danger")
		break;
	case AccountSign.NoneLimit:
		@("info")
		break;
	default:
		throw new NotImplementedException();
}
