@model MoveNature

@switch (Model)
{
	case MoveNature.Transfer:
		@("glyphicon glyphicon-transfer")
		break;
	case MoveNature.In:
	case MoveNature.Out:
	default:
		@await Html.PartialAsync("Extensions/Sign", Model == MoveNature.In)
		break;
}
