@model DFM.BusinessLogic.Response.TicketInfo
@if (Model.Current)
{
	@("warning")
}
else switch (Model.Type)
{
	case TicketType.Browser:
		@("info")
		break;
	case TicketType.Mobile:
		@("success")
		break;
	default:
		@("danger")
		break;
}
