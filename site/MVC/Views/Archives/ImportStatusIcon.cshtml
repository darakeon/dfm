@model ImportStatus
@switch (Model)
{
	case ImportStatus.Pending:
		@("hourglass")
		break;
	case ImportStatus.Success:
		@("ok")
		break;
	case ImportStatus.Error:
		@("remove")
		break;
	default:
		@("minus")
		break;
}
