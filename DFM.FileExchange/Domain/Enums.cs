namespace FileExchange.Domain
{
    public enum RecordStatus
    {
        Fail,
        Recorded,
        OnSystem,
    }

    public enum FileStatus
    {
        Fail,
        Recorded,
        Processing,
        OnSystem,
    }

}
