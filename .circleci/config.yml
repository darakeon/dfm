version: 2.1

jobs:
  cleaner:
    docker:
      - image: alpine:latest
    steps:
      - run: echo "This is a CI to clean branches in CircleCI"

workflows:
  version: 2.1
  clean:
    jobs:
      - cleaner
